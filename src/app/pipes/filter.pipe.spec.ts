import { FilterByTitlesPipe } from './filter.pipe';

describe('FilterByTitlesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByTitlesPipe();
    expect(pipe).toBeTruthy();
  });
});
